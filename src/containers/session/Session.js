import React from "react";
import { Box, Typography } from "@mui/material";

const Session = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography align="center" variant="h5">
       Session page goes here
      </Typography>
    </Box>
  );
};

export default Session;
